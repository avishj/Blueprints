# SPDX-FileCopyrightText: {{ copyright_year }} {{ author_name }} <{{ author_email }}>
#
# SPDX-License-Identifier: AGPL-3.0-or-later

set dotenv-load

default:
    @just --list

install:
    bun install --frozen-lockfile

lint:
    bunx --bun astro check
    bunx --bun tsc --noEmit

{% raw %}
test *args:
    bunx --bun vitest run {{ args }}
{% endraw %}

build:
    bunx --bun astro build

dev:
    bunx --bun astro dev

ci:
    just lint
    just test
    just build

clean:
    rm -rf dist/ .astro/ node_modules/ coverage/
